version: 2
updates:
  # Keep GitHub Actions pinned to current majors (checkout, upload-artifact,
  # cache). Review bumps like any other dependency change.
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
    labels:
      - dependencies
